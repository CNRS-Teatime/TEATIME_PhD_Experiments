import types

constants = types.SimpleNamespace(
    arango_db_image = "", # FIXME: replace with the real image when it's ready
    front_end_image = "", # FIXME: replace with the real image when it's ready
)